# Runtime dependencies. Upper bounds keep a surprise major release from
# breaking a working install; run `pip install -U -r requirements.txt` to move.
openai>=1.60.0,<2.0.0
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-dotenv>=1.0.1,<2.0.0
